# Results

We write sampled predictions under `predictions/`, metric JSON under `metrics/`, figures under `figures/`, and aggregate experiment tables at this directory level. Generated result files stay outside Git until we select the final report artifacts.
